요구사항
--------
2025-12-19 =====================================================================================================================


06:26 --------------------------------------------------------------------------------------------------------------------------

✅ 구현 완료 - 사이드바 메뉴에 도움말 툴팁 추가

**요구사항:**
- 사이드바의 각 메뉴에 `?` 모양의 아이콘 추가
- 아이콘에 마우스를 갖다 대면 해당 메뉴의 동작을 설명하는 툴팁 표시

**구현 내용:**

1. **도움말 아이콘 추가 (axon-sidebar.html)**
   - 각 섹션 헤더와 그룹에 `codicon-question` 아이콘 추가
   - `data-help` 속성으로 도움말 키 지정
   - 적용 메뉴:
     * Create Projects
     * Project Type
     * Build Option Extraction
     * Yocto Configuration
     * MCU Build
     * Yocto Build
     * DevTool
     * FWDN

2. **툴팁 스타일 (axon-sidebar.css)**
   - VS Code 네이티브 스타일 적용 (editorHoverWidget)
   - 반투명 효과 및 부드러운 전환
   - 최대 너비 350px, 여러 줄 지원
   - 화면 밖으로 나가지 않도록 위치 자동 조정

3. **툴팁 로직 (axon-sidebar.js)**
   - `helpMessages` 객체에 각 메뉴의 상세 설명 정의
   - 마우스 hover 시 툴팁 표시
   - 마우스 leave 시 100ms 딜레이 후 숨김
   - 화면 경계 감지 및 위치 자동 조정 (오른쪽/왼쪽/상단/하단)

4. **도움말 내용:**
   - **Create Projects**: 각 프로젝트 타입별 생성 방법 설명
   - **Project Type**: 프로젝트 타입 설정 및 메뉴 변경 설명
   - **Build Option Extraction**: bear make 및 IntelliSense 개선 설명
   - **Yocto Config**: 각 설정 파일의 용도 설명
   - **MCU Build**: MCU 빌드 옵션 및 명령어 설명
   - **Yocto Build**: Yocto 빌드 타입별 설명
   - **DevTool**: 외부 소스 관리 워크플로우 설명
   - **FWDN**: 펌웨어 다운로드 및 주의사항 설명

**사용 방법:**
- 사이드바의 `?` 아이콘에 마우스를 올리면 툴팁이 자동으로 표시됩니다.
- 툴팁은 화면 밖으로 나가지 않도록 자동으로 위치가 조정됩니다.

**수정 파일:**
- `axon-sidebar.html`: 도움말 아이콘 및 툴팁 컨테이너 추가
- `axon-sidebar.css`: 툴팁 스타일 정의
- `axon-sidebar.js`: 툴팁 표시/숨김 로직 및 도움말 메시지 정의


2025-12-18 =====================================================================================================================
18:30 --------------------------------------------------------------------------------------------------------------------------

✅ 구현 완료 - Yocto Project 생성에 Source Mirror & Buildtool Path 추가

**요구사항:**
- Yocto Project 생성 다이얼로그에 Source Mirror Path와 Buildtool Path 설정 추가
- 원격 환경(WSL/SSH) 지원
- Settings에 저장하여 재사용 가능

**구현 내용:**

1. **UI 구조:**
```
프로젝트 폴더 생성 및 Manifest git clone * [Browse] [경로] [Load]
⚙️ Build Tools (선택사항)
  - Source Mirror Path [Browse]
  - Buildtool Path [Browse]
SDK Manifest 선택 *
[생성]
```

2. **기능:**
- Browse 버튼으로 원격 경로 선택
- Settings에 저장 (`axon.yocto.sourceMirror`, `axon.yocto.buildtool`) - machine scope
- 프로젝트 생성 시 심볼릭 링크 자동 생성:
  - `{SDK폴더}/source-mirror` → Source Mirror Path
  - `{SDK폴더}/buildtools` → Buildtool Path
- 실패해도 프로젝트 생성 계속 진행

3. **수정 파일:**
- `yocto-dialog.html`: Build Tools 섹션 추가
- `yocto-dialog.css`: 섹션 스타일 추가
- `yocto-dialog.js`: Browse 버튼 핸들러, 데이터 전송
- `yocto/dialog.ts`: Browse 메시지 핸들러, Settings 읽기/쓰기
- `yocto/creator.ts`: 
  - `YoctoProjectData` 인터페이스에 `sourceMirrorPath`, `buildtoolPath` 추가
  - `createBuildToolsSymlinks()` 함수 구현

4. **사용 방법:**
- Settings (Ctrl+,)에서 "Axon: Source Mirror" 또는 "Axon: Buildtool" 검색하여 기본값 설정 가능
- 다이얼로그에서 Browse 버튼으로 개별 설정 가능
- 설정하지 않으면 auto-setup이 자동으로 다운로드

16:43 --------------------------------------------------------------------------------------------------------------------------

axon extension은 FWDN으로 인해 로컬과 원격 둘다 접근해서 구현하도록 되어 있잖아.
그래서 구현하면서 네가 path 관련한 오류를 굉장히 많이 하거든.. 
실수를 줄이게 하려면.. 빌드 관련된 것은 원격 전용으로 FWDN은 로컬용으로.. 만들어서..
빌드용 확장과 FWDN 확장이 같이 동작하면서 구현하는 것은 어떨까?? 

의견만 알려줘 구현은 하지말고


16:07 --------------------------------------------------------------------------------------------------------------------------
MCU Standalone Project 생성
은 지금 git clone command 하고 끝나잖아. 

git clone 한 디렉토리로 이동해야 해서.. 

make tcn100x_defconfig
make bootfw


까지 해줘..


15:51 --------------------------------------------------------------------------------------------------------------------------
axon.mcu.buildtoolpath 이거 왜.. 원격의 settings.json에 저장이 안되는거지??

15:00 --------------------------------------------------------------------------------------------------------------------------
MCU Standalone Project 생성에서 Build Tools Path를 Settings에서 관리 ✅

**요구사항:**
- Yocto Project (autolinux)처럼 Build Tools Path도 VS Code Settings에서 관리
- 사용자가 Settings에서 기본값 설정 가능

**구현:**
1. **package.json**: `axon.mcu.buildtoolPath` 설정 추가 (scope: "window")
2. **mcu/dialog.ts**: Settings에서 buildtoolPath 읽어서 WebView로 전송
3. **mcu-dialog.js**: init 메시지에서 buildtoolPath 받아서 input에 설정
4. **mcu-dialog.html**: Settings 안내 문구 추가

**사용 방법:**
- `Ctrl + ,` → "Axon: MCU Buildtool Path" 검색하여 기본값 설정
- 다이얼로그에서 Browse 버튼으로 개별 변경 가능

14:45 --------------------------------------------------------------------------------------------------------------------------
MCU Standalone Project 생성
⚙️ Build Tools
💡 선택사항: 이전에 다운로드한 경로를 설정하면 시간을 단축할 수 있습니다.

Build Tools Path
여기서 설정하는 tool path. 

Yocto Project (autolinux) 생성

에서 처럼.. 원격의 settings.json을 이용하는 로직을 추가해줘. 


14:45 --------------------------------------------------------------------------------------------------------------------------
MCU Standalone Project 생성 시 Build Tools 심볼릭 링크 생성 완료 ✅

**요구사항:**
- Build Tools Path가 설정되어 있으면 Git clone 후 심볼릭 링크 생성
- `<프로젝트>/tools` → Build Tools Path로 심볼릭 링크

**구현:**
- Git clone 완료 후 Build Tools Path 확인
- 기존 `tools` 폴더 제거: `rm -rf "<프로젝트>/tools"`
- 심볼릭 링크 생성: `ln -s "<buildtoolPath>" "<프로젝트>/tools"`
- 실패해도 프로젝트 생성은 계속 진행 (경고만 로그)

**수정 파일:**
- `mcu/dialog.ts`: `createProjectFromWebView()` 메서드에 심볼릭 링크 생성 로직 추가

**동작:**
1. Git clone 실행
2. Build Tools Path가 설정되어 있으면:
   - 기존 tools 폴더 제거
   - Build Tools Path로 심볼릭 링크 생성
3. 프로젝트 생성 완료

14:40 --------------------------------------------------------------------------------------------------------------------------
MCU Standalone Project 생성에서 Build Tools 심볼릭 링크 요청

Build Tools Path 설정 시 git clone 이후 `<프로젝트>/tools` 폴더에 심볼릭 링크 생성 


14:40 --------------------------------------------------------------------------------------------------------------------------
MCU Standalone Project 생성 레이블 변경 완료 ✅

**변경:**
- "프로젝트 폴더 *" → "프로젝트 폴더 생성 *"

**수정 파일:**
- `mcu-dialog.html`

**효과:**
- 사용자에게 폴더를 "생성"한다는 의도를 더 명확하게 전달

14:35 --------------------------------------------------------------------------------------------------------------------------
MCU Standalone Project 생성 레이블 변경 요청

프로젝트 폴더 * --> 프로젝트 폴더 생성 *


14:35 --------------------------------------------------------------------------------------------------------------------------
프로젝트 생성 다이얼로그의 안내 문구를 더 직관적으로 개선 ✅

**변경 전:**
- "💡 Browse로 폴더를 선택한 후 프로젝트 이름을 수정하고 Load를 클릭하세요"
- "💡 Browse로 폴더를 선택한 후 프로젝트 이름을 수정하세요"

**변경 후:**
- "💡 Browse로 상위 폴더를 선택하고, 경로 끝에 프로젝트 이름을 추가한 후 Load를 클릭하세요"
- "💡 Browse로 상위 폴더를 선택하고, 경로 끝에 프로젝트 이름을 추가하세요"

**수정 파일:**
- `autolinux-dialog.html`
- `yocto-dialog.html`
- `mcu-dialog.html`

**개선 효과:**
- "상위 폴더"라는 명확한 표현 사용
- "경로 끝에 프로젝트 이름을 추가"로 구체적인 행동 지시
- 사용자가 무엇을 해야 하는지 더 명확하게 이해 가능

14:30 --------------------------------------------------------------------------------------------------------------------------
MCU Standalone Project 생성과 Yocto Project 생성에서 경로 표시 방식 수정 완료 ✅

**문제:**
- Browse 버튼으로 선택한 경로가 URI 형식으로 표시됨
- 예: `vscode-remote://ssh-remote%2B7b22686f73744e616d65223a224465762d53657228eab09cebb09cec849cebb28429227d/home/B030240/work1/ext-test/zzz`

**해결:**
- `folders[0].toString()` → `folders[0].path`로 변경
- Unix 경로 형식으로 표시: `/home/B030240/work1/ext-test/zzz`

**수정 파일:**
- `mcu/dialog.ts`: `browseFolderForWebView()` 메서드
- `yocto/dialog.ts`: `browseFolderForWebView()` 메서드
- `autolinux-dialog.ts`: 이미 올바르게 구현되어 있음

14:25 --------------------------------------------------------------------------------------------------------------------------
MCU Standalone Project 생성과 Yocto Project 생성에서 경로 표시 방식 개선 요청

브라우즈해서 선택한 path를 
vscode-remote://ssh-remote%2B7b22686f73744e616d65223a224465762d53657228eab09cebb09cec849cebb28429227d/home/B030240/work1/ext-test/zzz 
이렇게 표시하지 말고 아래처럼 표시:
/home/B030240/work1/ext-test/zzz



13:50 --------------------------------------------------------------------------------------------------------------------------
MCU Standalone Project 생성에 Build Tools Path 추가 완료 ✅

**요구사항:**
- Yocto Project (autolinux) 생성과 동일한 UI 스타일
- Build Tools 섹션 추가
- "💡 선택사항: 이전에 다운로드한 경로를 설정하면 시간을 단축할 수 있습니다." 안내 문구
- Build Tools Path 입력 필드 및 Browse 버튼

**구현:**
- `mcu-dialog.html`: Build Tools 섹션 추가 (선택사항)
- `mcu-dialog.js`: `buildtoolInput` 변수 추가, `browseBuildtoolBtn` 이벤트 핸들러, `setBuildtoolPath` 메시지 핸들러
- `mcu-dialog.ts`: `browseBuildtoolForWebView` 메서드 추가, `createProjectFromWebView`에 buildtool 데이터 전달

13:45 --------------------------------------------------------------------------------------------------------------------------
MCU Standalone Project 생성에 Build Tools Path 추가 요청

아래에.. 
Build Tools 
💡 선택사항: 이전에 다운로드한 경로를 설정하면 시간을 단축할 수 있습니다.
라고 하고.. 
Build Tools Path
를 기입하도록 해줘.. 
Yocto Project (autolinux) 생성와 동일하게 


13:40 --------------------------------------------------------------------------------------------------------------------------
MCU Git URL도 VS Code Settings에서 관리되도록 구현 완료

- `axon.mcu.gitUrl` 설정이 이미 package.json에 추가되어 있음
- MCU Standalone Project 생성 다이얼로그에서 이 설정값을 사용
- 사용자는 `Ctrl + ,` → "Axon: MCU Git Url" 검색하여 수정 가능

13:30 --------------------------------------------------------------------------------------------------------------------------
MCU Standalone Project 생성에서 Clone 버튼 제거 및 생성 버튼에 통합

**변경 사항:**
1. Clone 버튼 제거
2. 생성 버튼이 Git clone과 프로젝트 설정을 모두 수행
3. UI 구조: Browse - Path - 생성 (3개 버튼)

**구현:**
- `mcu-dialog.html`: Clone 버튼 제거
- `mcu-dialog.js`: Clone 관련 코드 제거, 생성 버튼에 통합
- `mcu-dialog.ts`: `cloneProject` 메시지 핸들러 제거, `createProjectFromWebView`에 clone 로직 통합

13:19 --------------------------------------------------------------------------------------------------------------------------
MCU Standalone Project 생성에서.. 

새 브랜치 이름 (선택)
항목 없애줘.. 

12:26 --------------------------------------------------------------------------------------------------------------------------
MCU Standalone Project 생성에서.. 

browse해서..  폴더를 위치 결정하고. 폴더 이름을 추가해도

clone이라는 버튼이 활성화 되지 않네


12:12 --------------------------------------------------------------------------------------------------------------------------
Yocto Project 생성과 MCU Standalone Project 생성도.. 

프로젝트 이름 *
프로젝트 생성 위치 *
Git 저장소 URL *


이걸 Yocto Project (autolinux) 생성과 동일하게 해줘.. 


12:12 --------------------------------------------------------------------------------------------------------------------------
Yocto Project (autolinux) 생성
에서.. 
프로젝트 경로 *
이렇게 되어 있는데 아래 처럼 바꿔줘..
프로젝트 폴더 생성 및 autolinux git clone 

지금은 아래 처럼 구성되어 있는데.. 

<browse 버튼> -- <path 표시 창>  -- <git clone 버튼>

이렇게 해줘.. 

Autobuild script Git Repository URL * 부분은 위의 <git clone 버튼>으로 대체해줘.. 


12:08 --------------------------------------------------------------------------------------------------------------------------
Yocto Project (autolinux) 생성
git clone 하는 메뉴 아래에 있는 주의 문구 없애줘.. 


12:06 --------------------------------------------------------------------------------------------------------------------------
Yocto Project (autolinux) 생성에서 
Autobuild script Git Repository URL은 내부 setting 값으로 저장되어 있고.. 
이걸 기반으로 git clone 했으면 하는데.. 

url 수정은  settings (ctrl + ,) 에서 했으면 하고.. 
setting에도 extension 값들을 저장할 수 있다면 그렇게 했으면 하는데.. 

이것도 니가 이해한 바를 나한테 얘기해줘

10:15 --------------------------------------------------------------------------------------------------------------------------

Yocto Project (autolinux) 생성
프로젝트 경로 *
에서.. browse 버튼을 눌러서 폴더를 선택하고 창이 닫히면 커서의 위치가 
프로젝트 경로 *의 마지막 칸으로 가 있으면 하는데.. 

니가 이해한 바부터 설명해줘봐 .



09:41 --------------------------------------------------------------------------------------------------------------------------
autolinux의 with-subcore 옵션에 대해서 분석해서 알려줘 

08:50 --------------------------------------------------------------------------------------------------------------------------

원하는 것은 프로젝트 폴더 생성이야.. 
프로젝트 경로는 이미 생성되어 있는 폴더를 입력하라는 것 같은데.. 

08:41 --------------------------------------------------------------------------------------------------------------------------
Yocto Project (autolinux) 생성
에서..
프로젝트 이름 따로 프로젝트 생성 위치 따로 하는 것은 좀 이상한 것 같어.. 
한번에 생성하도록 바꿔줘


08:23 --------------------------------------------------------------------------------------------------------------------------
vscode에서 ctrl + , 해서 나오는 UI는 뭐야? Webview야 뭐야?

07:36 --------------------------------------------------------------------------------------------------------------------------
아니 잠깐만.. 이게 
"axon.projectType"는 
yocto project를 설정하면  yocto_project
yocto project(autolinux)를 설정하면 yocto_project_autolinux
이렇게 해줘 

07:13 --------------------------------------------------------------------------------------------------------------------------

Project Type 메뉴에서.. 
지금은 Yocto Project와 MCU project만 선택할 수 있는데.. 아래 항목 추가해줘
Yocto Project(autolinux)

그리고, Yocto Project(autolinux)를 선택하면. build -> MCU와 build -> Yocto 와 build -> DevTool은 안보이게 해줘..

그리고, Build -> Yocto(autolinux) 항목을 만들어줘 

Build -> Yocto 와 마찬가지로 선택 드롭 박스(?) 맞나? 이거 옆에 선택 버튼.. 나오게 해줘.. 

첫번째 항목으로 ./autolinux -c build 그대로 메뉴 이름으로 넣어주고 아래와 같이 구현해줘..

./autolinux -c build를 하면 선택하는 메뉴가 또 나오는데.. 
이걸 확장에서는 어떻게 구현하면 좋을까, 구현은 하지 말고 의견만 말해줘
는 아래와 같이 구현해줘..

 Extension 구현 방안 (3가지 옵션)
옵션 1: 간단한 QuickPick 방식 ⭐ (추천)
장점:
✅ 구현 간단 (기존 코드 재사용 가능)
✅ VS Code 네이티브 UI (vscode.window.showQuickPick)
✅ 빠른 선택 (키보드만으로 가능)
✅ 검색 기능 내장
단점:
❌ 이미지별 설명 표시 제한적
구현 개요:
```
// 1. SDK 템플릿에서 MainImages/SubImages 파싱 (이미 구현됨)
const images = await loadImages(projectPath, sdk, machine);

// 2. QuickPick으로 선택
const selected = await vscode.window.showQuickPick(
    images.map(img => ({
        label: img.name,
        description: img.date,
        detail: img.description  // 옵션
    })),
    { placeHolder: 'Select an image to build' }
);

// 3. 빌드 실행
await executeShellTask(
    `./autolinux -c build ${selected.label}`,
    projectPath
);
```
UI 예시:
┌─────────────────────────────────────────────────┐
│ Select an image to build                        │
├─────────────────────────────────────────────────┤
│ > telechips-ivi-image-minimal     up-to-date    │
│   telechips-ivi-image-multimedia  up-to-date    │
│   telechips-ivi-image             up-to-date    │
│   automotive-linux-platform-image up-to-date    │
│   telechips-ces2024-demo-image    up-to-date    │
└─────────────────────────────────────────────────┘

옵션 3: 하이브리드 방식 🚀 (최적)
QuickPick + 고급 옵션
장점:
✅ 기본은 간단하게 (QuickPick)
✅ 고급 사용자는 상세 설정 가능
✅ 단계적 구현 가능
구현 개요:
```
// 1단계: 빠른 선택 (QuickPick)
const quickItems = [
    { label: '$(rocket) Quick Build', description: 'Last built image' },
    { label: '$(list-unordered) Select Image...', description: 'Choose from list' },
    { label: '$(gear) Advanced...', description: 'Custom build options' }
];

const choice = await vscode.window.showQuickPick(quickItems);

if (choice.label.includes('Quick Build')) {
    // autolinux.config에서 마지막 이미지 읽어서 바로 빌드
    await buildImage(lastImage);
} 
else if (choice.label.includes('Select Image')) {
    // 이미지 목록 QuickPick
    const image = await selectImage();
    await buildImage(image);
}
else {
    // WebView 열기 (고급 옵션)
    showBuildDialog();
}
```
✨ 최종 추천
단계적 구현:
Phase 1 (기본): QuickPick으로 이미지 선택 → 빌드
Phase 2 (편의): Quick Build 버튼 추가
Phase 3 (고급): WebView로 상세 옵션 제공
이유:
사용자는 대부분 간단한 선택만 필요
복잡한 UI는 오히려 혼란 야기
단계적 구현으로 피드백 반영 가능
이렇게 구현하면 터미널 대화형 메뉴보다 훨씬 편리하고, VS Code 워크플로우에 자연스럽게 통합됩니다! 🎉

인데.. 일단, phase 1부터 진행할께..


07:09  --------------------------------------------------------------------------------------------------------------------------
./autolinux -c build를 하면 선택하는 메뉴가 또 나오는데.. 
이걸 확장에서는 어떻게 구현하면 좋을까, 구현은 하지 말고 의견만 말해줘


06:45
build version에서.. master qa release로 순서를 바꿔줘.. 

2025-12-17
16:52
그리고, 위 값을 설정하기 전에 원격의 settings.json에 있으면 그 값을 이용하고 메뉴는 나타나지 않고, 
 없으면 새로 설정하도록 메뉴가 나타나도록 해줘 

인데.. 아래와 같이 수정해줘
원격의 settings.json에 있으면 메뉴에서 사라지는 것은 아닌 것 같어.. 그 값(path)이 표시되고 사용될 수 있게 해줘..  만약, browse를 누르면 새로 설정하도록 해주고..

여기까지 구현했을 텐데 

settings.json에 있거나 또는 setting을 하기 위해 값을 넣었다면
해당 프로젝트에..  tools 라는 폴더만를 생성해줘.. 



12:17 

그리고, 위 값을 설정하기 전에 원격의 settings.json에 있으면 그 값을 이용하고 메뉴는 나타나지 않고, 
 없으면 새로 설정하도록 메뉴가 나타나도록 해줘 

인데.. 아래와 같이 수정해줘
원격의 settings.json에 있으면 메뉴에서 사라지는 것은 아닌 것 같어.. 그 값(path)이 표시되고 사용될 수 있게 해줘..  만약, browse를 누르면 새로 설정하도록 해주고..


12:04

SOURCE_MIRROR = ''  #set your downloaded source mirror path
BUILDTOOL = ''      #set your installed buildtools path

이거는 설정한 다음에. 원격에 있는 settings.json에 변수로 기록해줘..

그리고, 위 값을 설정하기 전에 원격의 settings.json에 있으면 그 값을 이용하고 메뉴는 나타나지 않고, 
 없으면 새로 설정하도록 메뉴가 나타나도록 해줘 

로컬(유저) settings.json 여기에 쓰는 건 아냐
원격 settings.json 여기에 쓰는거야..


10:48
Choose a Version

Manifest를 최신으로 선택한 경우(※ qa 빌드는 QA branch가 생성되었을 때만 빌드가 가능합니다.)

Choose Build Version
Choose a Version to build
  1.dev
  2.qa
Choose Version (1-2):1

라고 하는데.. 아까 하드코딩된거 말고 또 뭐가 있는거야?

10:33
다운로드 시간 줄이기
이전에 설치했거나 다운받아놓은 SDK의 buildtools, source-mirror가 있다면, 다운로드 시간을 현저히 줄일 수 있습니다.
template/sdk.py파일을 열어 SOURCE_MIRROR와 BUILDTOOL을  절대경로로 작성해주시기 바랍니다.

download build script
~/build-autolinux/$ vi template/sdk.py
 
SOURCE_MIRROR = ''  #set your downloaded source mirror path
BUILDTOOL = ''      #set your installed buildtools path
 
 
example)
SOURCE_MIRROR = '/home/B150247/dev/dunfell/source-mirror'
BUILDTOOL = '/home/B150247/dev/dunfell/buildtools'

이제 이 부분을 구현해야 할 것 같어.. 

이 부분은 ./autolinux -c configure 이전에 되어야 해..

버튼을 누르면 해당 폴더를 선택할 수 있도록 해줘. home 디렉토리 부터 찾아가도록 해서..
일단 네가 어떻게 구현할지 설명을 해주고, 내가 구현하라고 하면 시작해줘 

10:24 
refresh 버튼은 없애주고..   마지막 생성 & configure 버튼은 어떻게 해야 활성화 되는거지

10:07
실제 ./autolinux -c configure를 해보면


B030240@V124-UB20-B030240:~/work1/ext-test/origin/build-autolinux$ ./autolinux -c configure
The command is configure or Add configuration options(sdk,machine family,machine,manifest,feature)
Configure Start

Choose the Platform
  1.adas
  2.bsp
  3.cluster
  4.customer
  5.demo
  6.develop
  7.hvp
  8.ivi
  9.subcore
  10.topst
  11.topst-ai
Select SDK(1-11): 8
Choose the SDK to download
  1.tccxxxx_linux_ivi
  2.tcc897x_linux_ivi
  3.tcc802x_linux_ivi
  4.tcc803x_linux_ivi
  5.tcc805x_linux_ivi
  6.tcc807x_linux_ivi
  7.tcc805x_linux_consolidation
Select SDK(1-7): 6
Choose a Manifest to repo
  1.tcc807x_linux_ivi_k5.10_0.1.0.xml
  2.tcc807x_linux_ivi_k5.10_0.2.0.xml
  3.tcc807x_linux_ivi_k5.10_0.3.0.xml
Choose Manifest(1-3): 3
  1.tcc8070-main
  2.tcc8070-sub
Choose Machine(1-2): 1
Choose the Features at tcc8070-main
  1.  ufs                 Changed Boot Storage to UFS(supported : tcc8050, tsc8050, tcc8053, tcc8070, tcc8071, tsc8071)
  2.  bootchart           Install bootchart
  3.  secure-boot         Make Secure Signed Image
  4.  network             Install network packages
  5.  optee               Using OPTEE
  6.  str                 Enable Suspend To Ram
* 7.  with-subcore        Booting with sub-core
  8.  gpu-vz              GPU Virtualization
  9.  hud-display         Enable display for HUD
* 10. support-4k-video    Support 4k Video Contents
  11. booting-animation   Enable Booting Animation(Available on only one core, either Main or Sub)
* 12. meta-update         Enable Update
* 13. meta-micom          Enable Micom
  14. current             Enable feature for current measurement
  15. t-rvc               Enable Trust Rear Camera
  16. meta-apple          Enable Apple Features(iAP2, CarPlay)
  17. meta-hdradio        Enable HDR
  18. meta-drmradio       Enable DRM
  19. meta-dabradio       Enable DAB
  20. meta-commercial     Enable Commercial Codec
  0.Exit
Choose Features enable/disable (1-20): 0
Choose the Features at tcc8070-sub
  1.  ufs                 Changed Boot Storage to UFS(supported : tcc8050, tsc8050, tcc8053, tcc8070, tcc8071, tsc8071)
  2.  bootchart           Install bootchart
  3.  secure-boot         Make Secure Signed Image
  4.  str                 Enable Suspend To Ram
* 5.  rvc                 Enable Rear Camera
  6.  gpu-vz              GPU Virtualization
  7.  booting-animation   Enable Booting Animation(Available on only one core, either Main or Sub)
* 8.  meta-update         Enable Update
* 9.  meta-micom          Enable Micom
  10. current             Enable feature for current measurement(development)
  11. sdm                 Enable Safety Display Manager
  12. svm                 Enable Surround View Monitor
  13. t-rvc               Enable Trust Rear Camera
  14. android             Enable android feature
  0.Exit
Choose Features enable/disable (1-14): 0


이런식으로 진행되는데??

뭔가 다시 생각해야 하는 것 같어.. 

09:58
SDK Template은 정상적으로 보이는 것 같어.. 
그런데, Manifest와 Machine은 왜 TCN1000 관련 것들만 보이지


09:49

구현 사항 확인했는데. SDK Template *에.. Refresh 버튼을 누르든 뭘해도 select SDK에서 아무 것도 안뜨는데??

09:37
방안 1: 간소화된 WebView 다이얼로그 (추천 ⭐)
장점:
사용자 친화적인 UI
단계별 입력 가능
검증 및 에러 처리 용이
구현 내용:
┌─────────────────────────────────────────┐
│ Autolinux Configuration                 │
├─────────────────────────────────────────┤
│                                          │
│ SDK Template *                           │
│ [Select SDK...        ▼] [Refresh]      │
│                                          │
│ Manifest *                               │
│ [Select Manifest...   ▼]                 │
│                                          │
│ Machine *                                │
│ [Select Machine...    ▼]                 │
│                                          │
│ Build Version *                          │
│ ◯ Master  ◉ QA  ◯ Release               │
│                                          │
│ Features                                 │
│ ☑ with-subcore                          │
│ ☐ meta-update                           │
│ ☐ meta-flutter                          │
│ ☐ ...                                   │
│                                          │
│           [Cancel]  [Configure]          │
└─────────────────────────────────────────┘


동작 흐름:
WebView 다이얼로그 표시
"Refresh" 버튼 → template 폴더에서 SDK 목록 읽기
SDK 선택 → 해당 SDK의 Machine 목록 로드
Machine 선택 → Feature 목록 로드
"Configure" 버튼 → ./autolinux -c configure 실행

이렇게 구현해 주는데.. 기존의 Yocto Project (autolinux) 생성의 아래쪽에 구현하는 것은 어때?


09:31

## 구현 항목 2.
그 다음에는 make autolinux configuration file이라는 버튼을 누르면

./autolinux -c configure
에서의 항목들을 구현해야 해..

일단. 니가 한번 보고 어떻게 구현할지를 검토해서 나한테 알려줘봐.. 구현은 하지 말고


09:17
ref/autolinux는 원격 환경(빌드 서버)에 프로젝트 워크스페이스에 존재하는 빌드 관련 스크립트야..
이제부터는 autolinux를 커맨드들을 실행 시키기는 기능들을 구현할꺼야..

## 구현 항목 1. 

시작은 create projects에 하나의 항목을 더 추가해줘

MCU Standalone Project
Yocto Project
Yocto Project (autolinux)  <-- 새로 추가되는 것..


Yocto Project (autolinux)를 선택하면

Yocto Project와 동일하게 webview 형태로 창을 하나 띄워줘..

Yocto Project 생성과 동일하게..  
프로젝트 이름
프로젝트 생성 위치 메뉴를 만들어줘.. 

그리고, Manifest Git 저장소 URL * 대신에.. 
Autobuild script Git Repository URL을 넣어주고.. 
해당 주소는  ssh://bitbucket.telechips.com:7999/script/build-autolinux 이야.. 

그 옆(해당 주소)의 load라는 버튼을 누르면.. 위에서 생성한 프로젝트 위치에.. 
git clone ssh://bitbucket.telechips.com:7999/script/build-autolinux를 해줘..

그 다음에.. SDK Manifest 선택 *이 있을텐데.. 이건 없애줘.. 











2025-12-12
10:44
생성되는 팝업 창의 종류가 어떤 것이 있지? 방금 전의 요구 사항에 의해 만들어지는 팝업 창 말고 다른 건 없나?

답변: 현재 구현된 팝업 창 종류:

1. **터미널 닫기 확인 팝업** (요구사항 10:04에서 요청)
   - Yocto 빌드/클린 완료 시: ✅ 구현됨 (askToCloseTerminal 함수)
   - FWDN 완료 시: ✅ 구현됨 (fwdn.ts)
   - MCU 빌드 완료 시: ❌ 아직 미구현 (요구사항에 따라 추가 필요)

2. **사용자 확인 팝업** (showWarningMessage)
   - FWDN 실행 확인: "FWDN (펌웨어 다운로드)을 실행하시겠습니까?"
   - FWDN Low Level Format 확인: "FWDN Low Level Format을 실행하시겠습니까? ⚠️ 경고: 이 작업은 eMMC와 SNOR의 모든 데이터를 영구적으로 삭제합니다!"
   - Yocto 빌드 시작 확인: "빌드를 시작하시겠습니까?" (MACHINE, SDK VERSION 정보 포함)
   - MCU 빌드 시작 확인: "빌드를 시작하시겠습니까?" (코어 정보 포함)
   - 프로젝트 타입 변경 확인: ❌ 아직 미구현 (요구사항 15:53에서 요청)

3. **에러 메시지 팝업** (showErrorMessage)
   - 워크스페이스 폴더 없음
   - Boot Firmware 폴더를 찾을 수 없음 (다시 시도 옵션 포함)
   - 빌드/실행 중 오류 발생
   - 설정 검증 실패
   - Bear 설치 필요 (Build Option Extraction)

4. **정보 메시지 팝업** (showInformationMessage)
   - 작업 시작 알림: "{작업명}가 시작되었습니다. 터미널을 확인하세요."
   - 작업 완료 알림: "{작업명}가 완료되었습니다!"
   - 작업 취소 알림: "{작업명}이 취소되었습니다."
   - 경로 설정 완료: "{경로명} 경로가 설정되었습니다: {경로값}"

5. **선택 팝업** (showQuickPick)
   - 레시피 선택 (DevTool)
   - 머신 선택 (Yocto 빌드: tcn1000, tcn100x 등)
   - SDK 버전 선택 (Yocto 빌드: dev, qa 등)
   - MCU 코어 선택 (m7-0, m7-1, m7-2, m7-np)
   - 파티션 선택 (FWDN 파티션 다운로드)

6. **입력 팝업** (showInputBox)
   - 레시피 이름 입력 (DevTool create-workspace)

7. **폴더 선택 팝업** (showOpenDialog)
   - 프로젝트 생성 시 폴더 선택 (MCU/Yocto)
   - DevTool finish 시 layer 폴더 선택


10:04
build, clean, fwdn이 완료되었을 때.. 완료되었습니다.터미널을 닫겠습니까? 라는 팝업창을 띄우주고.. yes를 누르면 해당 터미널을 닫아줘 


07:07

FWDN이 옵션이 어릴 때.. 
Example:
  Start to fwdn(load FWDN F/W) :
    fwdn --fwdn tcc805x_fwdn.json
  Download files written to a config file :
    fwdn --write tcc805x_boot.json
  Download file :
    fwdn -w [file name] -m emmc --area boot0
    fwdn -w [file name] -m emmc --area boot1 -s 512
  Download file at GPT partition:
    fwdn -w [file name] -m emmc --area user --part boot
    fwdn -w [file name] -m ufs --area user --part boot --sector-size 4096
  Read dump :
    fwdn -r [file name] -m emmc --area boot1 --size 0x100 --addr 0x123456
    fwdn -r [file name] -m emmc --area boot1 --size 0x100
  Erase storage :
    fwdn --low-format --storage [storage type]
  Print storage info
    fwdn --device-info

여기에서 나는 이것을 구현하고 싶어... 
  Download file at GPT partition:
    fwdn -w [file name] -m emmc --area user --part boot



일단 위 사항을 구현하기 위한 요구 사항이야..
1. FWDN의 -> run FWDN / low level format 아래에.. 메뉴를 추가해야 해..
2. available image file이라는 버튼을 클릭하면..
3. {yocto root}/build/tcn1000/tmp/deploy/images/tcn1000/ 폴더에서.. partition.list 파일을 읽어야 해.. 
4. 읽으면 아래와 같은 포맷일꺼야.. 


bl3_main_a:2M@/home/B030240/work1/sv/slave/test-slave-branch/build-axon/linux_yp4.0_cgw_1.x.x_dev/build/tcn1000/tmp/deploy/images/tcn1000/u-boot-tcn1000.rom
bl3_main_b:2M@/home/B030240/work1/sv/slave/test-slave-branch/build-axon/linux_yp4.0_cgw_1.x.x_dev/build/tcn1000/tmp/deploy/images/tcn1000/u-boot-tcn1000.rom
boot_a:30M@/home/B030240/work1/sv/slave/test-slave-branch/build-axon/linux_yp4.0_cgw_1.x.x_dev/build/tcn1000/tmp/deploy/images/tcn1000/tc-boot-tcn1000.img
boot_b:30M@/home/B030240/work1/sv/slave/test-slave-branch/build-axon/linux_yp4.0_cgw_1.x.x_dev/build/tcn1000/tmp/deploy/images/tcn1000/tc-boot-tcn1000.img
system_a:288M@/home/B030240/work1/sv/slave/test-slave-branch/build-axon/linux_yp4.0_cgw_1.x.x_dev/build/tcn1000/tmp/deploy/images/tcn1000/telechips-cgw-image-tcn1000.ext4
system_b:288M@/home/B030240/work1/sv/slave/test-slave-branch/build-axon/linux_yp4.0_cgw_1.x.x_dev/build/tcn1000/tmp/deploy/images/tcn1000/telechips-cgw-image-tcn1000.ext4
dtb_a:200K@/home/B030240/work1/sv/slave/test-slave-branch/build-axon/linux_yp4.0_cgw_1.x.x_dev/build/tcn1000/tmp/deploy/images/tcn1000/tcn1000-lpd4x321.dtb
dtb_b:200K@/home/B030240/work1/sv/slave/test-slave-branch/build-axon/linux_yp4.0_cgw_1.x.x_dev/build/tcn1000/tmp/deploy/images/tcn1000/tcn1000-lpd4x321.dtb
misc:1M@
home:500M@/home/B030240/work1/sv/slave/test-slave-branch/build-axon/linux_yp4.0_cgw_1.x.x_dev/build/tcn1000/tmp/deploy/images/tcn1000/home-directory.ext4
env:1M@
data:0M@


그럼 아래와 같이 선택 메뉴를 만들어줘.
만들 때.. partition.list를 파싱해서 만들어줘야 해..  위 예제에서는 12개가 만들어져야 해.. 
근데.. misc와 data는 의미 없으니 빼줘
bl3_main_a (tcn1000/u-boot-tcn1000.rom)
.
.
.


그 다음에..  위 항목 중 하나를 선택하면..

fwdn -w {path}/{위 의 파일명} -m emmc --area user --part <앞부분( ex) bl3_main.a, boot.a 등.>

를 실행해야 해..

이건 bat파일을 이용해서 구현해야 할꺼야..

아래가 내가 테스트해서 잘 동작했던. bat 파일이야.. 참고해

@echo off
set component_file_PATH=Z:\work1\sv\slave\test-slave-branch\build-axon\linux_yp4.0_cgw_1.x.x_dev\build\tcn1000\tmp\deploy\images\tcn1000

::set boot-FAI_PATH=Z:\work1\cesrom\boot-firmware_tcn1000\axon1\


set boot-firmware_PATH=Z:\work1\sv\slave\test-slave-branch\build-axon\linux_yp4.0_cgw_1.x.x_dev\boot-firmware_tcn1000

 
:: Step 1. Connect FWDN
fwdn --fwdn %boot-firmware_PATH%\tcn100x_fwdn.json
 
:: Step 2. Download SNOR rom file
::fwdn -w %boot-firmware_PATH%\tcn100x_snor.rom --storage snor --area die1
 
:: Step 3. Download boot partition images to eMMC
fwdn -w %boot-firmware_PATH%\tcn100x_boot.json
 
:: Step 4. Download FAI file to eMMC
::fwdn -w %boot-firmware_PATH%\SD_Data.fai --storage emmc --area user
 
fwdn -w %component_file_PATH%\tc-boot-tcn1000.img -m emmc --area user --part boot_a
 
::fwdn --low-format --storage emmc
::fwdn --low-format --storage snor
@pause




2025-12-01
16:24
생성된 comiple_commands.json
@define.py를 실행시켜서 comiple_commands.json을 읽어서. c_cpp_properties.json 에 define을 추가해야 해..
c_cpp_properties.json이 없으면, 이것을 생성해줘야 해.. 그리고 기본 양식을 추가해 줘야 해..
c_cpp_properties.json이 있으면, 기존의 define을 삭제하고 comiple_commands.json의 값들로 덮어 씌워야 해.. 

원격에서 실행을 위해서는 vscode에서 python 코드를 임시로 만들고 실행하고 삭제하는 형식이어야 해, 방금 구현한 공통 함수를 사용해서 구현해줘



15:53
프로젝트 타입이 MCU 프로젝트 일때 나타나는 항목이야.. 
프로젝트 타입 선택하는 바로 밑에 있어야 해

Build Option Extraction이라는 메뉴이고..
그걸 선택하면.. mcu project 루트에서.. 
bear make를 실행해야 해..
만약, bear가 설치가 안되어 있다면 설치를 해줘야 하고..
그 다음에.. compile_commands.json 생성되는 것까지 구현해줘. 


15:21
FWDN 실행 메뉴에.. low level format 항목을 추가해줘..

FWDN 섹션 아래에.. 
Run FWDN과 low level 선택할 수 있는 드롭박스가 뜨고.. 
옆에는 플레이 아이콘이 있고.. 선택된 것을 실행하도록..


low level format을 하게 되면.. fwdn_all.bat 파일에서..  step 1까지 실행하고.. 그 다음에..  아래를 실행해야 해..

fwdn --low-format --storage emmc
fwdn --low-format --storage snor


12:59
devtool finish <recipe> <layer>에서.. layer는 현재 워크스페이스의 루트를 시작으로 해서 검색해서 선택하게 해줘 


12:28
devtool 메뉴의 devtool update-recipe (recipe)는 필요 없는 기능 처럼 보여.. 
대신. devtool finish <recipe> <layer> 형태를 구현해줘..
devtool finish <recipe> 까지는 기존에 구현된 devtool build (recipe)나 devtool update-recipe (recipe)를 참고하고.. <layer>는 파일 탐색기를 열어서 폴더를 선택할 수있게 해줘. .



09:50 
devtool 메뉴의 devtool build (recipe) 부분 아래에 
devtool에서 update-recipe 부분을 구현해줘..
위와 마찬가지로 devtool update-recipe <recipe>라고 글씨를 써주고.
그 아래에 레시지를 선택하게 해줘. 레시피는 devtool build와 마찬가지로 settings.json에 가져오도록 해주고.. 레시피를 선택하면 위와 마찬가지로 devtool update-recipe <recipe>를 수행해줘..


09:21 
devtool에서 creat & modify에서.. 
레시피를 선택하고. create & modify를 누르면 다시 drop box가 나오는데.. 왜 drop box가 나오지?


09:10
devtool 관련 루틴은 src/projects/yocto 폴더 아래에. devtool.ts 라는 파일을 만들고
거기에 다 모아놓아줘.

08:43
yocto/mcu root 폴더 처럼.. bootfirmware path도 settings.json에 있으면 그 값을 바로 사용하고 없으면 찾아서 저장하도록 해줘


08:30

fwdn.ts의 executeFwdnCommand() 함수에서.. buildAxonFolderName를 찾고 있는데, 이것은 필요 없는 부분인 것 같어..

그리고, bootFirmwareFolderName를 넣을 필요는 굳이 없어.. 
그리고, bootFirmwareFolder를 찾는 루틴은 yocto/mcu 프로젝트에서 root를 폴더를 찾는 것과 유사항 방법으로 찾았으면 해.. 
찾는 방법은 아래와 같아.
find . -maxdepth 4 -name prebuilt 라는 폴더를 찾고, 이 폴더의 상위 폴더가 bootFirmwareFolder야..

이 정보를 기반으로 fwdn.ts를 전반적으로 수정해줘..


08:10

yocto 프로젝트 root 폴더를 찾을 때나 mcu 프로젝트 root를 찾을 때.. 리눅스 쉘의 find . -maxdepth 4 -name poky 이런식으로 썼었잖아..

이거 공통 함수로 묶을 수 있지 않을까 싶은데

06:54

executeMcuTask() 함수도..  executeYoctoBuildTask와 비슷한 구조였으면 해. 


getYoctoProjectRoot()와 유사하게.. linux shell을 통해서 muc project의 root를 찾아줬으면 해.. 
먼저 find . -maxdepth 4 -name tcn100x_defconfig .. 에서.. 
./mcu-tcn100x/build/configs/tcn100x_defconfig
이렇게 결과가 나올텐데.. 여기에서.. mcu project의 root는 
./mcu-tcn100x/ 여기야. 상위의 상위 폴더야.  

이렇게 


2025-11-28
----------
14:28 
임시 파일은 삭제하도록 되어 있나?

14:25
쉘 스크립트가 파일은 /home/B030240/work1/ext-test/slt-test/build-axon/linux_yp4.0_cgw_1.0.0_qa 처럼.. yocto root에 생성하고..

vscode가 이 파일을 워크스페이스 루트에서 찾아서 문제가 발생하고 있네.. 


✅ 완료 - 12:33
Yocto 프로젝트 루트 찾기 로직 개선 (리눅스 쉘 명령어 사용):
- getYoctoProjectRoot 함수에서 poky 디렉토리를 찾아 상위 디렉토리를 프로젝트 루트로 사용
- 리눅스 쉘 명령어 직접 실행: find . -maxdepth 2 -name poky -not -path "*/.repo/*"
- 원격 환경(WSL/SSH)에서도 작동하도록 vscode.tasks API 사용
- 임시 스크립트 파일 생성 → 실행 → 결과 파일 읽기 방식
- boot-firmware 폴더 대신 poky 디렉토리 기반 탐색으로 변경
- findPokyDirectory 헬퍼 함수 추가 (쉘 명령어 실행 방식)
- Linter 오류 없음 


✅ 완료 - 11:24
프로젝트 타입 선택 시 settings.json 업데이트 및 webview 동기화 구현:
- utils.ts에 setProjectType 함수 추가 (프로젝트 타입 및 관련 폴더명 자동 설정)
- extension.ts에 axon.setProjectType 명령 등록
- 프로젝트 타입 설정 후 webview에 상태 동기화 (sendProjectType 호출)
- 프로젝트 타입 선택 시 settings.json에 "axon.projectType" 업데이트
- 관련 폴더명(buildAxonFolderName, bootFirmwareFolderName)도 자동 설정
- Linter 오류 없음 


✅ 완료 - 11:21
프로젝트 타입 선택 UI는 항상 표시되도록 수정:
- Configuration 섹션은 항상 표시 (프로젝트 타입 선택을 위해)
- Build, FWDN 섹션만 projectType이 없을 때 숨김 처리
- Configuration 섹션 내 Yocto 설정 그룹도 projectType이 없을 때 숨김 처리
- Linter 오류 없음

✅ 완료 - 11:16
axon 확장을 클릭했을 때, settings.json에 axon.projectType이 없으면 Build, FWDN 섹션 숨김 처리:
- HTML에 섹션 ID 추가 (section-configurations, section-build, section-fwdn)
- updateProjectTypeUI 함수에서 projectType이 없을 때 Build, FWDN 섹션만 숨김 처리
- 초기 로드 시에도 projectType 확인하여 섹션 표시/숨김 처리
- Linter 오류 없음

✅ 완료 - 10:39
setupApEnvironmentOnly / setupMcuEnvironmentOnly 제거 완료:
- 두 함수가 하던 환경 설정 작업은 getBuildCommands에서 이미 수행 중 (중복)
- executeYoctoBuildTask: envPath 확인과 buildDir 계산만 수행하도록 간소화
- editApLocalConf: setupBuildDirectoryWithCgwScript 직접 호출
- editMcuLocalConf: mcu-build.sh 실행 코드 직접 구현
- Linter 오류 없음

10:31
setupMcuEnvironmentOnly나 setupApEnvironmentOnly이거 이제 필요한 거야??

변경된 함수에서 이미 하고 있지 않나??

아니.. 그게. 아니고..  setupApEnvironmentOnly 이 함수는 결국은.. 
@builder.ts (436-438) 를 하고 싶은 거고.. 
setupMcuEnvironmentOnly 이 함수도 결국 @builder.ts (320-322) 를 하고 싶은 거잖아.. 

그런데.. 

buildAp에서는  @builder.ts (1001-1003) 

이렇게.. 되어 있고.. 
buildMcu에서는 
@builder.ts (1043-1044) 이렇게 되어 있는 것 같아서..

결국 같은 것으로 보여서.. 

✅ 완료 - 09:56 
가독성을 위해서.. 
@builder.ts (844-859) 이런식으로 command 처리했는데,,  buildKernel, buildAp 함수도 그렇게 바꿔줘..

buildAp, buildMcu, buildKernel 모두 템플릿 리터럴 형태로 변경 완료:
- getBuildCommands가 문자열 반환하도록 인터페이스 수정
- 공통 함수에서 모든 빌드 타입을 동일하게 처리
- AP/MCU/Kernel 모두 fullCommand 템플릿 리터럴 형태로 통일


✅ 완료 - 09:55
가독성을 위해서.. 
@builder.ts (844-859) 이런식으로 command 처리해줘..

cleanApBuild, cleanMcuBuild, cleanAllBuild 함수의 getCleanCommand를
배열 join(' && ') 형태에서 멀티라인 템플릿 리터럴 형태로 변경 완료

✅ 완료 - 09:41
project/yocto/builder.ts에도.. buildAp(), buildMcu(), buildKernel(), cleanApBuild(), cleanMcuBuild(), cleanAllBuild() 이것도 공토함수를 만들고.. 그것을 사용하는 구조로 해줘..
아까 mcu쪽 수정했던 것 참고해서..

Yocto builder 리팩토링 완료:
- YoctoBuildTaskConfig, YoctoCleanTaskConfig 인터페이스 정의
- executeYoctoBuildTask, executeYoctoCleanTask 공통 함수 생성
- buildAp, buildMcu, buildKernel → 공통 빌드 함수 사용
- cleanApBuild, cleanMcuBuild, cleanAllBuild → 공통 클린 함수 사용
- 코드 중복 대폭 감소 (약 600줄 → 200줄)
- Linter 오류 없음 

✅ 완료 - 09:34
mcu build는 project/mcu/ 폴더의 builder.ts로 이동하는 것이 좋을 것 같은데.. 

✅ 완료 - 09:24
executeMcuTask에서도.. 
yocto의 @builder.ts (923-938) 처럼..  command 인자를 전달했으면 해.. 

✅ 완료 - 09:19
executeMcuTask 함수는 진짜로 공통적인 부분만 있어야 할 것 같은데.... 

@extension.ts (1184-1252)  @extension.ts (1135-1152) 는　공통적인　부분은　아닌　것　같아．．
이것을　호출하는　곳에서　인자로써　전달해야　할　것　같어．　

✅ 완료 - 08:56
executeMcuClean, executeMcuBuildAll, executeMcuBuildMake 이 함수 세개는 거의 비슷해 보이는데..
리팩토링을 해야 할 것 같은데..

✅ 완료 - mcu build를 project/mcu/builder.ts로 이동
MCU build 관련 코드를 projects/mcu/builder.ts의 McuProjectBuilder 클래스로 이동 완료:
- executeMcuTask → McuProjectBuilder.executeMcuTask (private)
- executeMcuBuildMake → McuProjectBuilder.buildMake
- executeMcuBuildAll → McuProjectBuilder.buildAll
- executeMcuClean → McuProjectBuilder.cleanBuild
- 폴더 검색 함수들도 모두 이동
- extension.ts는 이제 McuProjectBuilder를 import하여 사용

완료했습니다! ✅ 세 함수를 리팩토링하여 코드 중복을 제거했습니다.

## 변경 사항:

### 리팩토링 결과:

* 이전: 270줄 (3개 함수, 각 90줄씩)
* 이후: 약 180줄 (1개 공통 함수 + 3개 래퍼 함수)
* 코드 감소: 약 90줄 (~33% 감소)

### 구조:

1. executeMcuTask(config) - 공통 로직을 처리하는 핵심 함수

* 인터페이스 McuTaskConfig로 작업 타입 정의
* 모든 공통 부분 통합:
* 환경 정보 로깅
* 빌드 폴더 검색
* 사용자 확인
* 명령 실행
* 에러 처리
* 작업별 차이점만 분기 처리

1. 래퍼 함수들 - 기존 인터페이스 유지

* executeMcuBuildMake() → executeMcuTask({ taskType: 'build', core })
* executeMcuBuildAll() → executeMcuTask({ taskType: 'buildAll' })
* executeMcuClean() → executeMcuTask({ taskType: 'clean' })

### 장점:

* ✅ 코드 중복 제거
* ✅ 유지보수성 향상 (한 곳만 수정하면 됨)
* ✅ 기존 함수 시그니처 유지 (호환성 보장)
* ✅ 타입 안전성 (TypeScript interface 사용)
* ✅ 확장성 향상 (새로운 작업 타입 추가 용이)

8:00

Build <인자>가 선택되면.. 인자가 executeMcuBuildMake 함수에 전달되어야 해..
이를테면 Build m7-np가 선택되면.. m7-np 라는 인자가 executeMcuBuildMake 함수에 전달되어야 해..

그리고, 최종 실행 커맨드는
make tcn100x_<인자>_defconfig && make
이를테면..
make tcn100x_m7-np_defconfig 이어야 해.

7:01
⏱️ [PERF] state-restore: 0.10ms
axon-sidebar.js:12 ⏱️ [PERF] event-delegation-setup: 0.00ms
axon-sidebar.js:12 ⏱️ [PERF] dom-ready: 0.00ms
axon-sidebar.js:24
📊 ============ Performance Report ============
axon-sidebar.js:25 🎯 Total Loading Time: 3.30ms
axon-sidebar.js:26 📋 Breakdown:
axon-sidebar.js:32    - state-restore: 0.10ms
axon-sidebar.js:32    - dom-ready: 0.00ms
axon-sidebar.js:32    - event-delegation-setup: 0.00ms
axon-sidebar.js:35 ============================================

axon-sidebar.js:12 ⏱️ [PERF] update-state: 0.10ms
axon-sidebar.js:12 ⏱️ [PERF] update-recipes: 0.10ms

이게 결과이긴 한데..

내가 눈으로 확장 아이콘 클릭하고.. 모든 webview의 화면 구성이 완료되는데. 2~3초 걸리는 것 같은데.

6:56
알았어.. 하이브리드 비추천은 알겠어..
그럼.. 나는 로딩 속도 프로파일링을 좀 했으면 좋겠어..
네가 말하는 것처럼 50ms 인지 총 얼마나인지.
프로파일링할 수 있게 구현해줘

6:33
다른 확장을 클릭을 하고 axon 확장으로 돌아오고 로딩하는데 시간이 오래 걸리는데.. 왜지?

6:29
axon 확장에서 작업을 하고, 다른 확장을 클릭하고.. 다시 axon으로 돌아오면. 이전에 했던 상태가 유지되었으면 하는데..
무슨 뜻인지는 이해가 되나?
--------------------------

2025-11-27
----------

16:29
FWDN 메뉴를 Configuration과 Build과 동일한 레벨의 항목으로써 가장 아래에 위치하도록 해줘..

16:19

Webview가 열릴 때,, vscode가 새로 시작하거나, 다른 확자 아이콘을 클릭하고 다시 axon 확장 아이콘을 클릭 했을 때..
settings.json의 axon.devtool.recipes 의 값도 읽어와야 해..

그리고, Devtool 메뉴의 devtool Create-workspace & devtool modify <recipe>
와 레시피 선택과 실행 버튼 아래에..

devtool build <recipe>라는 글을 써주고.
settings.json의 axon.devtool.recipes의 값들을 선택하게 해주고.. 그걸 선택하면 .. build 실행되도록 만들어줘

16:13

1. Create Project에서..
   MCU Standalone Proejct 와 Yocto Proejct를 선택할 수 있게 해주고.. 옆아 아이콘을 누르면 실행되로고 해줘..

16:08

1. Devtool에서..
   create & modify도 마찬가지로.. 레시피들을 선택할 수 있는 드롭박스가 나오고.. 그 옆에.. play 버튼이 나오도록 해줘..
   선택할 수 있는 레시피는
   linux-telechips
   m7-0
   m7-1
   m7-2
   m7-np
   tpa-app
   dpi-app
   u-boot-tcc

드랍박스 위에.. devtool Create-workspcae & devtool modify <recipe> 라고 써줘

16:08

1. mcu project가 선택되었을 때.. configuration의 yocto는 안보여져야해

15:57

1. 다른 extension 아이콘을 선택했다고 axon extension 아이콘을 선택하면..
   settings.json을 읽어서, current:에 현재 project type을 표시해야 해야 함.. (지금은 그렇지 않음.)
   vscode를 새로 열때만 settings.json을 읽도록 되어 있어..

15:53

1. configuration 아래에.. set project type을 선택할 수 있는 UI를 만들어줘.. yocto_project, 또는 mcu_project
   그리고, 왼쪽에 적용 버튼을 만들어 주고, 그것을 선택하면..
   settings.json의 "axon.projectType"의 값이 바뀌도록 해줘..
   근데 적용하기 전에 팝업으로 물어봐야 해.. 진짜 바꿀껀지..
2. Webview가 열릴 때.. settings.json을 해당 값을 읽어서.. 있다면 current project type 위 선택 UI가 해당 값을 가리키도록 해줘..
   그리고, 어딘가에 current project is yocto project 라고 표시해줬으면 좋겠어..
3. 그리고, yocto project 라면. Build 메뉴 아래 mcu build 관련 항목을 보여주지 마..
4. 그리고, mcu proejct 라면, build yocto, devtool관련 항목을 보여주지마.

15:33

오.. Run Build 와 Run Clean은 재생 버튼 같은 아이콘으로 변경해줘

네, 버튼의 형태는 CSS를 통해 얼마든지 다양하게 변경할 수 있습니다. 현재 사용 중인 버튼은 VS Code의 표준 스타일을 따르고 있어 다소 평범한 직사각형 형태입니다.
공간을 더 효율적으로 쓰거나 스타일을 바꾸고 싶다면 다음과 같은 대안들이 있습니다:
아이콘 버튼 (추천): 텍스트("Run Build")를 없애고 아이콘(▶)만 표시하여 정사각형 모양으로 만듭니다. 공간을 가장 적게 차지하며 깔끔합니다. (마우스를 올리면 툴팁으로 기능 설명 가능)
원형 버튼 (Pill): 모서리를 완전히 둥글게 깎아 알약 모양으로 만듭니다.
고스트 버튼 (Ghost): 평소에는 배경색 없이 아이콘/글자만 보이다가, 마우스를 올렸을 때만 배경이 나타납니다.
현재 "상자 크기"를 줄이고 싶어 하셨으므로, 1번(아이콘 버튼) 형태로 변경하는 것이 가장 효과적일 것 같습니다.
아이콘 버튼으로 변경해드릴까요? 원하시면 바로 적용해드리겠습니다.

15:10

1. Run Build 상자 폭을 지금 보다 작게 해줘 80% 정도로.
2. Build MCU 쪽도 비슷한 컨셉으로 구현 해줘
   Build All, Build m7-0, Build m7-1, Build m7-2, Build m7-n를 선택하도록 해주고..
   Run Build를 통해 실행되도록

15:06

1. Run Build와 Run Clean의 상자 크기를 동일하게 해줘...   Run Build에 맞춰줘.
2. 그리고 상자의 모서리가 둥글게 해줘

15:00
clean AP, clean MCU, Clean ALL 도 마찬가지로 구현해줘

---

14:55
옵션 선택 하는 부분 (Build AP, Build MCU, Build, Kernel) 바로 옆에 run bulid 버튼이 생기게 해줘.. 바로 아래 말고

---

14:46
webview UI의 yocto --> build를 선택하면 Drop box 나오고..  Build AP, Build MCU, Build Kernel이 나오고
거시서 해당 아이템을 선택하고 실행하게 해줘
-------------------------------------------

에서.. inc file을 수정하는 것은..
해당 변수의 값이 ${AUTOREV}일때만 하는 것으로 해줘..

---

2025-11-27

---

13:56

devtool modify 할 때..

@extension.ts (886-890)

실행 단계:

1. devtool create-workspace (workspace가 없을 때만)
2. devtool modify
3. bbappend 파일 수정

이렇게 실행하도록 되어 있는데..
3번 과정을 아래로 대체할께..

devtool modify를 하고 나면.. external-workspace/sources/<레시피명>
이라는 폴더가 생길꺼야..

거기서 git의 가장 최근 commit id를 복사해..
그리고,

telechips-cgw-rev.inc 파일을 읽어서.
if (레피시명 = linux-telechips)
KERNEL_BRANCH_DEV_SRC = 여기에는 받아 놓은 git의 Head commit id로 설정..
else if (레시피명 = m7-0 or  m7-1 or  m7-2 or m7-np)
MCU_BRANCH_DEV = = 여기에는 받아 놓은 git의 Head commit id로 설정..
else if (레시피명 = dpi-app)
DPI_APP_BRANCH_DEV_SRC = = 여기에는 받아 놓은 git의 Head commit id로 설정..
else if (레시피명 = tpa-app)
TPA_APP_BRANCH_DEV_SRC = = 여기에는 받아 놓은 git의 Head commit id로 설정..
else if (레시피명 = u-boot-tcc)
UBOOT_BRANCH_DEV_SRC = = 여기에는 받아 놓은 git의 Head commit id로 설정..
