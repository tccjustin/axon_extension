# Axon Extension 구현 사항

## 📅 2025-12-17

### ✅ 구현 항목 1: Yocto Project (autolinux) 프로젝트 생성 및 Configuration 기능 (완전한 WebView 구현)

---

### ✅ 구현 항목 2: Build Tools & Source Mirror 경로 설정 기능 + User Settings 저장 (2025-12-17 추가)

#### 개요
다운로드 시간 단축을 위해 이전에 설치했거나 다운받아놓은 SDK의 buildtools와 source-mirror 경로를 설정할 수 있는 기능을 추가했습니다. **설정된 경로는 원격 서버의 User Settings에 저장되어 재사용**되며, `sdk.py` 파일에 자동으로 반영됩니다.

#### 구현 내용

**1. UI 추가 (Load 후, Configuration 전)**

```
┌─────────────────────────────────────────────┐
│ ⚙️ Build Tools & Source Mirror               │
├─────────────────────────────────────────────┤
│ 💡 선택사항: 다운로드 시간 단축              │
│                                              │
│ Source Mirror Path                           │
│ [/home/username/source-mirror  ] [Browse]    │
│                                              │
│ Build Tools Path                             │
│ [/home/username/buildtools     ] [Browse]    │
└─────────────────────────────────────────────┘
```

**2. 동작 흐름 (User Settings 자동 저장/로드)**

1. **Load 버튼 클릭** → `git clone` 실행
2. **User Settings 확인** (원격 서버의 `~/.vscode-server/data/Machine/settings.json`)
   - 저장된 경로 있음 → 자동으로 input 필드에 표시 ✅
   - 저장된 경로 없음 → input 필드 비어있음
   - **Build Tools 섹션 항상 표시** (저장 여부와 관계없이) ✅
3. **경로 확인 및 수정**
   - 저장된 경로 그대로 사용 → 다음 단계로
   - Browse 버튼 클릭 → 새 경로 선택 → User Settings 자동 업데이트 ✅
4. **Platform/SDK 선택**
5. **생성 & Configure 클릭**
   - ✅ User Settings 또는 input 필드에서 경로 읽기
   - ✅ `sdk.py` 파일의 SOURCE_MIRROR, BUILDTOOL 값 업데이트
   - ✅ `./autolinux -c configure` 실행
   - ✅ Workspace settings.json 생성 및 프로젝트 열기

**3. sdk.py 파일 수정**

**수정 전:**
```python
SOURCE_MIRROR = ''  # 빈 문자열
BUILDTOOL = ''      # 빈 문자열
```

**수정 후:**
```python
SOURCE_MIRROR = '/home/B150247/dev/dunfell/source-mirror'
BUILDTOOL = '/home/B150247/dev/dunfell/buildtools'
```

**4. 구현 기능**

**package.json**
```json
{
  "contributes": {
    "configuration": {
      "properties": {
        "axon.yocto.sourceMirror": {
          "type": "string",
          "default": "",
          "description": "Source Mirror 경로 (Yocto 다운로드 시간 단축)",
          "scope": "machine"  // 원격 서버별로 저장
        },
        "axon.yocto.buildtool": {
          "type": "string",
          "default": "",
          "description": "Build Tools 경로 (Yocto 빌드 도구 재사용)",
          "scope": "machine"
        }
      }
    }
  }
}
```

**autolinux-dialog.html**
- Build Tools & Source Mirror 섹션 추가
- Source Mirror Path input + Browse 버튼
- Build Tools Path input + Browse 버튼
- help-text로 선택사항임을 명시

**autolinux-dialog.js**
- `sourceMirrorPath`, `buildtoolPath` 변수 추가
- Browse 버튼 이벤트 핸들러
- **`autolinuxLoaded` 메시지에서 저장된 경로 확인**
  - 저장된 경로가 있으면 자동으로 input 필드에 표시 ✅
  - 없으면 input 필드 비어있음
  - **Build Tools 섹션 항상 표시** (저장 여부와 관계없이) ✅
  - 사용자가 언제든지 경로 확인 및 수정 가능 ✅
- 경로 설정 메시지 수신 (`setSourceMirrorPath`, `setBuildtoolPath`)
- `createProject` 메시지에 경로 데이터 포함

**autolinux-dialog.ts**
```typescript
// Load 시 User Settings에서 저장된 경로 로드
private async loadAutolinuxForWebView(...)
    → git clone 실행
    → User Settings 확인
      const config = vscode.workspace.getConfiguration('axon.yocto');
      const savedSourceMirror = config.inspect<string>('sourceMirror')?.globalValue;
      const savedBuildtool = config.inspect<string>('buildtool')?.globalValue;
    → WebView로 전송

// 폴더 선택 다이얼로그 함수 + User Settings 저장
private async browseSourceMirrorForWebView(panel: vscode.WebviewPanel)
    → 홈 디렉토리를 기본 경로로 폴더 선택
    → User Settings에 저장
      await config.update('sourceMirror', folderPath, vscode.ConfigurationTarget.Global);
    → 선택한 경로를 WebView로 전송

private async browseBuildtoolForWebView(panel: vscode.WebviewPanel)
    → 홈 디렉토리를 기본 경로로 폴더 선택
    → User Settings에 저장
      await config.update('buildtool', folderPath, vscode.ConfigurationTarget.Global);
    → 선택한 경로를 WebView로 전송
```

**autolinux-creator.ts**
```typescript
// 인터페이스 확장
export interface AutolinuxProjectData {
    // ... 기존 필드
    sourceMirror?: string;  // 추가
    buildtool?: string;     // 추가
}

// sdk.py 수정 함수 추가
static async updateSdkPyPaths(
    projectPath: vscode.Uri,
    sourceMirror?: string,
    buildtool?: string
): Promise<void>
    → sdk.py 파일 읽기
    → 정규식으로 SOURCE_MIRROR, BUILDTOOL 값 치환
    → 파일 쓰기
    → 로그 출력

// createAutolinuxProject에서 호출
if (sourceMirror || buildtool) {
    await this.updateSdkPyPaths(projectFullUri, sourceMirror, buildtool);
}
// ↓ Configuration 실행 (sdk.py가 이미 수정된 상태)
```

#### 주요 특징

✅ **선택사항**: 경로를 설정하지 않아도 프로젝트 생성 가능
✅ **자동 저장/로드**: 한 번 설정하면 원격 서버의 User Settings에 저장되어 다음부터 자동 표시
✅ **원격 서버별 저장**: SSH/WSL 서버마다 다른 경로 저장 가능 (`scope: "machine"`)
✅ **항상 표시**: Build Tools 섹션이 항상 표시되어 저장된 경로 확인 및 수정 가능 ✨
✅ **유연한 수정**: Browse 버튼으로 언제든지 새 경로 설정 가능
✅ **자동 수정**: Configuration 전에 sdk.py 파일 자동 업데이트
✅ **tools 폴더 자동 생성**: 경로 설정 시 `tools` 더미 폴더 생성으로 FTP 다운로드 완전 차단 🆕
✅ **홈 디렉토리 시작**: Browse 시 홈 디렉토리부터 탐색 시작
✅ **원격 환경 지원**: WSL/SSH 환경에서 경로 정확히 처리
✅ **정규식 기반**: 유연한 패턴 매칭으로 sdk.py 수정
✅ **에러 처리**: 파일 읽기/쓰기 실패 시 명확한 에러 메시지

#### 기술적 구현

**정규식 패턴:**
```typescript
const sourceMirrorRegex = /SOURCE_MIRROR\s*=\s*['"].*['"]/;
const buildtoolRegex = /BUILDTOOL\s*=\s*['"].*['"]/;
```

**홈 디렉토리 처리 (원격 환경 지원):**
```typescript
// 현재 workspace의 URI scheme을 사용하여 원격 환경 지원
const workspaceFolder = vscode.workspace.workspaceFolders?.[0];
if (workspaceFolder) {
    const homeDir = process.env.HOME || '/home';
    defaultUri = vscode.Uri.from({
        scheme: workspaceFolder.uri.scheme,      // ssh://, vscode-remote:// 등
        authority: workspaceFolder.uri.authority, // 원격 호스트 정보
        path: homeDir
    });
}
```

**원격 경로 처리:**
```typescript
const folderPath = folders[0].scheme === 'file' 
    ? folders[0].fsPath 
    : folders[0].path;
```

#### 사용 예시

**최초 사용:**
```
1. Load 클릭 → Build Tools 섹션 표시
   - Source Mirror Path: [비어있음] [Browse]
   - Build Tools Path: [비어있음] [Browse]

2. Source Mirror Browse → /home/B150247/dev/dunfell/source-mirror 선택
3. Build Tools Browse → /home/B150247/dev/dunfell/buildtools 선택
4. User Settings에 자동 저장 ✅
```

**두 번째 사용 이후:**
```
1. Load 클릭 → Build Tools 섹션 표시 (항상 표시!) ✅
   - Source Mirror Path: [/home/B150247/dev/dunfell/source-mirror] [Browse]
   - Build Tools Path: [/home/B150247/dev/dunfell/buildtools] [Browse]

2. [옵션 1] 저장된 경로 그대로 사용 → 다음 단계로
   [옵션 2] Browse로 새 경로 선택 → User Settings 자동 업데이트 ✅

3. sdk.py 파일이 자동으로 수정됨
   SOURCE_MIRROR = '/home/B150247/dev/dunfell/source-mirror'
   BUILDTOOL = '/home/B150247/dev/dunfell/buildtools'

4. 다운로드 시간 단축! ⚡
```

**저장 위치:**
```
원격 SSH: ~/.vscode-server/data/Machine/settings.json
WSL: ~/.vscode-server-wsl/data/Machine/settings.json
```

**핵심 개선:**
- ✨ Build Tools 섹션이 항상 표시되어 저장된 경로를 언제든지 확인 가능
- ✨ Browse 버튼으로 경로를 쉽게 변경 가능 (다른 SDK로 전환할 때 유용)
- ✨ **tools 폴더 자동 생성으로 FTP 다운로드 완전 차단** 🆕

### 🔧 tools 폴더 생성의 중요성

**autolinux의 다운로드 조건:**
```python
# ref/autolinux 라인 1310-1313
if not SourceMirrors:
    if not os.path.isdir('source-mirror/4.0') or not os.path.isdir('tools'):
        download_mirror(path, Distro)  # FTP 다운로드!
```

**문제:**
- `SOURCE_MIRROR` 설정해도 `tools` 폴더가 없으면 **or 조건** 때문에 다운로드 시도!

**해결:**
- Extension이 `tools` 더미 폴더를 자동 생성
- `source-mirror/4.0` 심볼릭 링크 + `tools` 폴더 존재 → 다운로드 완전 차단! ✅

#### 개요
Create Projects 메뉴에 "Yocto Project (autolinux)" 옵션을 추가하고, autolinux 빌드 스크립트 기반 프로젝트 생성 기능을 구현했습니다.

#### 구현 내용

**1. Create Projects 메뉴 확장**
- MCU Standalone Project
- Yocto Project
- **Yocto Project (autolinux)** ⬅ 신규 추가

**2. WebView 다이얼로그 구현**

```
┌─────────────────────────────────────────┐
│ Yocto Project (autolinux) 생성           │
├─────────────────────────────────────────┤
│ 프로젝트 이름 *                          │
│ 프로젝트 생성 위치 *                     │
│ Autobuild script Git Repository URL *   │
│   └ ssh://bitbucket.../build-autolinux  │
│   └ [Load] 버튼                         │
└─────────────────────────────────────────┘
```

**3. 동작 흐름**
1. 프로젝트 이름 입력
2. 프로젝트 생성 위치 선택 (Browse)
3. Load 버튼 클릭 → `git clone` 실행
4. 생성 버튼 클릭 → 새 VSCode 창 열기

**4. 수정된 파일**
- `src/webview/axon-sidebar.html`: "Yocto Project (autolinux)" 옵션 추가
- `src/webview/axon-sidebar.js`: 명령어 라우팅 추가
- `src/webview/autolinux-dialog.html`: 다이얼로그 UI
- `src/webview/autolinux-dialog.css`: 스타일링
- `src/webview/autolinux-dialog.js`: UI 로직
- `src/projects/yocto/autolinux-creator.ts`: 프로젝트 생성 로직
- `src/projects/yocto/autolinux-dialog.ts`: WebView 관리
- `src/extension.ts`: 명령어 등록

---

### ✅ 구현 항목 1-1: 완전한 Autolinux Configuration 기능 재설계 (2025-12-17 업데이트)

#### 개요
실제 `./autolinux -c configure` 실행 과정을 완벽하게 반영한 WebView UI 구현. Platform 선택부터 동적 Feature 로딩까지 전체 워크플로우를 지원합니다.

#### 구현 내용

**1. 완전한 Configuration UI**

```
┌─────────────────────────────────────────────┐
│ 📦 Autolinux Configuration                   │
├─────────────────────────────────────────────┤
│ Platform *                                   │
│ [Select Platform...      ▼]                  │
│   (ivi, cluster, adas, subcore, bsp 등)      │
│                                              │
│ SDK Template *                               │
│ [Select SDK...           ▼]                  │
│   (선택한 Platform의 SDK만 표시)              │
│   (Load 후 자동 로드)                        │
│                                              │
│ Manifest *                                   │
│ [Select Manifest...      ▼]                  │
│                                              │
│ Machine *                                    │
│ [Select Machine...       ▼]                  │
│                                              │
│ Build Version *                              │
│ ◉ QA  ◯ Master  ◯ Release                   │
│                                              │
│ tcc8070-main Features (Machine 선택 후 표시) │
│ ☑ ufs          Changed Boot Storage to UFS  │
│ ☐ bootchart    Install bootchart           │
│ ☑ with-subcore Booting with sub-core        │
│ ...                                          │
│                                              │
│ tcc8070-sub Features (Sub machine이 있으면)  │
│ ☑ rvc          Enable Rear Camera           │
│ ☐ gpu-vz       GPU Virtualization           │
│ ...                                          │
│                                              │
│ [생성 & Configure]  (모든 필수 항목 입력 시 활성화) │
└─────────────────────────────────────────────┘
```

**2. 실제 autolinux 프로세스를 반영한 동작 흐름**

1. **Load 버튼 클릭** 
   → `git clone build-autolinux` 실행
   → Configuration 섹션 활성화
   → 자동으로 Platform/SDK 목록 로드 (`sdk.py` 파싱)

2. **Platform 선택** (예: ivi, cluster, adas)
   → 해당 Platform의 SDK 목록으로 자동 필터링
   → 예: ivi 선택 시 → tccxxxx_linux_ivi, tcc807x_linux_ivi 등만 표시

3. **SDK 선택** (예: tcc807x_linux_ivi)
   → 해당 SDK의 Manifest와 Machine 목록 자동 로드
   → SDK 템플릿 파일 (`tcc807x_linux_ivi.py`) 파싱

4. **Manifest 선택** (예: tcc807x_linux_ivi_k5.10_0.3.0.xml)

5. **Machine 선택** (예: tcc8070-main)
   → **동적 Feature 로딩 시작**
   → `classes/feature.py`의 `Feature` 클래스 사용
   → Chipset, SDK, Manifest date 기반으로 Feature 목록 생성
   → Main Machine Features 표시
   → Sub Machine이 있으면 Sub Machine Features도 표시

6. **Features 선택**
   - Main Machine Features: 체크박스로 선택
   - Sub Machine Features: 별도 섹션에서 선택
   - 기본값(enabled=True) 자동 체크
   - Feature 설명 툴팁 표시

7. **Build Version 선택** (QA/Master/Release)

8. **생성 & Configure 버튼 클릭**
   → `./autolinux -c configure --sdk <sdk> --manifest <manifest> --machine <machine> --buildversion <ver> --features <main_features> --subfeatures <sub_features>` 실행
   → settings.json 생성
   → 새 VSCode 창 열기

**생성 & Configure 버튼 활성화 조건:**
- ✅ 프로젝트 이름 입력
- ✅ 프로젝트 경로 선택
- ✅ Git URL 입력
- ✅ Load 성공
- ✅ Platform 선택
- ✅ SDK 선택
- ✅ Manifest 선택
- ✅ Machine 선택
- ⚠️ Features는 선택 안 해도 됨 (선택 사항)

**3. 주요 구현 사항**

**A. Frontend (WebView)**

**autolinux-dialog.html**
- **Platform select** 추가: Platform 선택 UI
- **SDK select + Refresh**: Platform에 따라 필터링된 SDK 목록
- **Manifest/Machine select**: SDK에 따라 로드
- **Main/Sub Features 섹션 분리**: 동적으로 표시/숨김
- Features는 Machine 선택 후 동적으로 생성

**autolinux-dialog.js**
- `updatePlatformSelect()`: Platform 목록 표시
- `updateSdkSelectForPlatform()`: Platform 선택 시 SDK 필터링
- `updateMainFeaturesCheckboxes()`: Main Machine Features 동적 생성
- `updateSubFeaturesCheckboxes()`: Sub Machine Features 동적 생성
- Feature 데이터 구조: `{name, enabled, desc}`
- 체크박스 기본값(enabled) 자동 반영
- 메시지 핸들링: `platformsAndSdksLoaded`, `featuresLoaded` 등

**B. Backend (TypeScript)**

**autolinux-creator.ts**
```typescript
// 인터페이스 업데이트
export interface AutolinuxProjectData {
    projectName: string;
    projectUri: vscode.Uri;
    autolinuxGitUrl: string;
    platform?: string;           // 추가
    sdkTemplate?: string;
    manifest?: string;
    machine?: string;
    buildVersion?: string;
    mainFeatures?: string[];     // 변경 (features → mainFeatures)
    subFeatures?: string[];      // 추가
}

// 핵심 함수들
static async loadPlatformsAndSdks(projectPath: vscode.Uri): Promise<{[platform: string]: string[]}>
    → sdk.py를 파싱하여 Platform별 SDK 딕셔너리 반환
    → Python 스크립트 실행: from sdk import SDK

static async loadManifestsAndMachines(projectPath: vscode.Uri, sdkTemplate: string)
    → SDK 템플릿 파일 (예: tcc807x_linux_ivi.py) 파싱
    → Manifests: [[xml, date], ...] 형식에서 xml 추출
    → Machines: list 또는 dict 형식 처리

static async loadFeatures(projectPath: vscode.Uri, sdkTemplate: string, manifest: string, machine: string)
    → ⭐ 가장 복잡한 함수
    → classes/feature.py의 Feature 클래스 사용
    → Feature(chipset, manifest_date, sdkTemplate) 인스턴스 생성
    → feature_obj.getFeatureList('main') / getFeatureList('sub') 호출
    → 반환: {mainFeatures: [{name, enabled, desc}], subFeatures: [...]}

static async runAutolinuxConfigure(...)
    → --features <main_features> --subfeatures <sub_features> 추가
```

**autolinux-dialog.ts**
```typescript
// 메시지 핸들러 추가/변경
private async refreshPlatformsAndSdksForWebView(...)
    → loadPlatformsAndSdks() 호출
    → 결과를 'platformsAndSdksLoaded' 메시지로 전송

private async loadFeaturesForWebView(...)
    → loadFeatures() 호출
    → 결과를 'featuresLoaded' 메시지로 전송
```

**4. 실행 명령어 구성 (업데이트)**

```bash
cd build-autolinux
./autolinux -c configure \
    --sdk <sdkTemplate> \
    --manifest <manifest> \
    --machine <machine> \
    --buildversion <buildVersion> \
    --features <main_feature1,main_feature2,...> \
    --sub-features <sub_feature1,sub_feature2,...>
```

**예시:**
```bash
cd build-autolinux
./autolinux -c configure \
    --sdk tcc807x_linux_ivi \
    --manifest tcc807x_linux_ivi_k5.10_0.3.0.xml \
    --machine tcc8070-main \
    --buildversion qa \
    --features ufs,with-subcore,support-4k-video,meta-update,meta-micom \
    --sub-features rvc,meta-update,meta-micom
```

**중요**: `--sub-features` (하이픈 포함), `--subfeatures` ❌

**5. Settings.json 저장 항목**

```json
{
    "axon.projectType": "yocto_project",
    "axon.yocto.projectRoot": "<projectPath>",
    "axon.yocto.isAutolinux": true,
    "axon.yocto.autolinux.sdk": "<sdkTemplate>",
    "axon.yocto.autolinux.machine": "<machine>",
    "axon.yocto.autolinux.buildVersion": "<buildVersion>"
}
```

#### 핵심 기능 특징

✅ **완전한 autolinux 워크플로우 구현**: 실제 `./autolinux -c configure` 대화형 프로세스를 WebView로 완벽 재현
✅ **Platform 기반 필터링**: Platform 선택 시 해당 SDK만 표시 (UX 개선)
✅ **동적 Feature 로딩**: Machine, SDK, Manifest date에 따라 Python Feature 클래스로 동적 생성
✅ **Main/Sub Machine 구분**: -main과 -sub Machine의 Feature를 별도 섹션으로 구분 표시
✅ **Feature 기본값 지원**: enabled=True인 Feature 자동 체크
✅ **Feature 설명 툴팁**: 각 Feature의 desc를 툴팁으로 표시
✅ **Python 스크립트 통합**: `sdk.py`, `feature.py` 등 autolinux의 Python 코드 직접 활용
✅ **원격 환경 지원**: WSL/SSH 환경에서 `vscode.workspace.fs` API로 완벽 동작
✅ **단계별 검증**: Platform → SDK → Manifest → Machine → Features 순서로 점진적 활성화

#### Python 통합 및 동적 데이터 로딩

**✅ 완전 구현 완료**: autolinux의 Python 코드를 직접 실행하여 모든 데이터를 동적으로 로드

**1. Platform/SDK 로딩 (`loadPlatformsAndSdks`)**
```python
# sdk.py에서 Platform별 SDK 딕셔너리 import
from sdk import SDK
# SDK = {
#     'ivi': ['tccxxxx_linux_ivi', 'tcc807x_linux_ivi', ...],
#     'cluster': ['tcc805x_linux_cluster', ...],
#     ...
# }
print(json.dumps(SDK))
```

**2. Manifest/Machine 로딩 (`loadManifestsAndMachines`)**
```python
# SDK 템플릿 파일에서 Manifests, Machines import
sdk_module = __import__('tcc807x_linux_ivi')
manifests = sdk_module.Manifests  # [[xml, date], ...]
machines = sdk_module.Machines    # list or dict
# [[xml, date], ...] → [xml, ...] 변환
result['manifests'] = [m[0] for m in manifests]
```

**3. Feature 로딩 (`loadFeatures`) ⭐ 핵심**
```python
# classes/feature.py의 Feature 클래스 사용
from feature import Feature

# SDK에서 manifest date 찾기
sdk_module = __import__('tcc807x_linux_ivi')
manifest_date = 'up-to-date'
for m in sdk_module.Manifests:
    if m[0] == 'tcc807x_linux_ivi_k5.10_0.3.0.xml':
        manifest_date = m[1]  # '2023/07/07'
        break

# Feature 인스턴스 생성 (chipset, date, sdk 기반)
chipset = 'tcc8070'  # machine.split('-')[0]
feature_obj = Feature(chipset, manifest_date, 'tcc807x_linux_ivi')

# Feature 목록 가져오기
main_features = feature_obj.getFeatureList('main')
sub_features = feature_obj.getFeatureList('sub')

# 반환: [[name, enabled, desc], ...]
result = {
    'mainFeatures': [{'name': f[0], 'enabled': f[1], 'desc': f[2]} for f in main_features],
    'subFeatures': [{'name': f[0], 'enabled': f[1], 'desc': f[2]} for f in sub_features]
}
```

**Feature 클래스의 역할**:
- `classes/features/*.py` 파일들에서 Feature 정의 로드
- chipset, sdk, date에 따라 적용 가능한 Feature만 필터링
- Feature 간 의존성(dependency) 처리
- 각 Feature의 상태(enabled), 설명(desc) 제공

**버그 수정 히스토리**:
1. **SDK Template 로딩 실패**: `vscode.Uri.file()` → `vscode.Uri.joinPath()` (원격 환경 지원)
2. **SDK 데이터 파싱 실패**: 속성 이름 수정 (`SupportedManifestList` → `Manifests` 등)
3. **Feature 구현 변경**: 하드코딩된 Feature → Python Feature 클래스로 동적 생성
4. **Sub-features 옵션 이름**: `--subfeatures` ❌ → `--sub-features` ✅ (하이픈 필요)
5. **Browse 버튼 원격 경로**: `vscode.Uri.file()` → `vscode.Uri.from()` (workspace의 scheme/authority 사용)

---

## 파일 구조

```
axon_extension/
├── src/
│   ├── extension.ts                              # 명령어 등록
│   ├── webview/
│   │   ├── axon-sidebar.html                     # 메인 사이드바 (프로젝트 선택)
│   │   ├── axon-sidebar.js
│   │   ├── autolinux-dialog.html                 # Autolinux 프로젝트 생성 다이얼로그
│   │   ├── autolinux-dialog.css
│   │   └── autolinux-dialog.js
│   └── projects/
│       └── yocto/
│           ├── autolinux-creator.ts              # 프로젝트 생성 로직
│           └── autolinux-dialog.ts               # WebView 관리
└── out/
    └── webview/                                  # 컴파일된 webview 파일
```

---

## 빌드 및 테스트

```bash
cd axon_extension
npm run compile
```

컴파일 성공 후:
1. F5 키로 Extension Development Host 실행
2. Axon 사이드바에서 "Create Projects" 선택
3. "Yocto Project (autolinux)" 선택
4. 다이얼로그에서 프로젝트 정보 입력
5. Load → Configuration 설정 → 생성 & Configure

---

---

## 📊 최종 구현 요약

### 주요 성과
✅ **실제 autolinux 프로세스 완벽 재현**: Platform → SDK → Manifest → Machine → Features 전체 흐름
✅ **Python 코드 직접 통합**: `sdk.py`, `feature.py` 등 autolinux의 Python 로직 활용
✅ **동적 Feature 생성**: Feature 클래스로 chipset/sdk/date 기반 Feature 자동 생성
✅ **원격 환경 완벽 지원**: WSL/SSH에서 `vscode.workspace.fs` API로 안정적 동작
✅ **자동 로딩**: Load 후 Platform/SDK 목록 자동 로드 (Refresh 버튼 불필요)
✅ **Build Tools 최적화**: Source Mirror와 Build Tools 경로 설정으로 다운로드 시간 단축
✅ **자동 설정 파일 수정**: sdk.py 파일을 정규식으로 자동 업데이트

### 기술적 하이라이트
- **복잡한 Python 통합**: TypeScript에서 Python 스크립트 생성/실행/파싱
- **임시 파일 방식**: 원격 환경에서 Python 출력 캡처 (.temp_*.py, .temp_*_output.json)
- **단계별 데이터 로딩**: 각 선택에 따라 다음 단계 데이터 동적 로드
- **UI/UX 최적화**: 기본값 자동 체크, 툴팁 표시, 단계별 활성화

### 수정된 파일 (총 4개)
1. `src/webview/autolinux-dialog.html`: Platform 선택, Main/Sub Features 섹션, Build Tools 섹션 추가
2. `src/webview/autolinux-dialog.js`: Platform 필터링, Feature 동적 생성, Build Tools 경로 처리
3. `src/projects/yocto/autolinux-creator.ts`: 
   - Platform/SDK/Features 로딩 함수
   - **sdk.py 수정 함수 (`updateSdkPyPaths`) 추가**
   - AutolinuxProjectData 인터페이스 확장
4. `src/projects/yocto/autolinux-dialog.ts`: 
   - 메시지 핸들러 추가
   - **폴더 선택 다이얼로그 함수 (`browseSourceMirror`, `browseBuildtool`) 추가**

---

---

## 🎯 프로젝트 경로 입력 개선

### 개요

프로젝트 이름과 생성 위치를 별도로 입력하던 방식을 **통합 경로 입력**으로 개선했습니다.

### 변경 사항

#### **Before (분리 방식):**
```
프로젝트 이름: my-project
프로젝트 생성 위치: /home/user/workspace
→ 최종 경로: /home/user/workspace/my-project
```

#### **After (통합 방식):**
```
프로젝트 경로: /home/user/workspace/my-project
→ 프로젝트 이름: my-project (자동 추출)
```

### 장점

1. ✅ **간단함**: 한 번에 입력
2. ✅ **명확함**: 최종 경로를 바로 확인
3. ✅ **유연함**: Browse로 선택 후 마지막 폴더명만 수정
4. ✅ **VS Code 표준**: 다른 Extension과 동일한 방식

### 구현 내용

**autolinux-dialog.html**
- 프로젝트 이름 필드 제거
- 프로젝트 경로 필드를 편집 가능하게 변경
- 도움말 텍스트 추가

**autolinux-dialog.js**
```javascript
// 프로젝트 경로에서 프로젝트 이름 자동 추출
const fullPath = projectPathInput.value.trim();
const projectName = fullPath.split('/').filter(p => p).pop() || 'autolinux-project';
```

**수정된 파일 (2개):**
1. `autolinux-dialog.html`: UI 개선
2. `autolinux-dialog.js`: 자동 추출 로직

---

## 📦 Autolinux 빌드 기능 (Phase 1)

### 개요

Autolinux 프로젝트의 빌드 기능을 구현했습니다. QuickPick UI를 사용하여 이미지를 선택하고 빌드를 실행할 수 있습니다.

### 구현 내용

#### 1. **UI 추가**

**axon-sidebar.html**
- Project Type에 "Yocto Project (autolinux)" 옵션 추가
- Build 섹션에 "Yocto (autolinux)" 그룹 추가
- `./autolinux -c build` 옵션 제공

**axon-sidebar.js**
- `updateProjectTypeUI()` 함수 확장
  - `yocto_autolinux` 프로젝트 타입 처리
  - autolinux 프로젝트일 때:
    - ✅ Yocto(autolinux) 빌드 메뉴 표시
    - ❌ MCU 빌드 메뉴 숨김
    - ❌ Yocto 빌드 메뉴 숨김
    - ❌ DevTool 메뉴 숨김
    - ❌ Yocto Config 메뉴 숨김
- `btn-yocto-autolinux-run-build` 버튼 이벤트 핸들러 추가

#### 2. **백엔드 구현**

**autolinux-creator.ts**
```typescript
// SDK 템플릿에서 MainImages/SubImages 로딩
static async loadImages(projectPath, sdk, machine)
    → SDK 템플릿 파일(.py) 읽기
    → Python 스크립트로 MainImages/SubImages 파싱
    → 임시 파일 방식으로 JSON 출력
    → 결과 파싱 및 반환
    
    반환값: {
        mainImages: [{ name: string, date: string }],
        subImages: [{ name: string, date: string }]
    }
```

**autolinux-builder.ts** (신규 파일)
```typescript
class AutolinuxProjectBuilder {
    static async buildAutolinux()
        → 워크스페이스 확인
        → autolinux.config 파일 읽기
        → SDK, Machine 추출
        → loadImages()로 이미지 목록 로드
        → Machine이 sub인지 확인 (mainImages vs subImages)
        → QuickPick으로 이미지 선택
        → ./autolinux -c build [image] 실행
}
```

**extension.ts**
- `axon.buildAutolinux` 명령어 등록
- AutolinuxProjectBuilder 동적 import 및 실행

#### 3. **동작 흐름**

```
1. 사이드바에서 "Yocto (autolinux)" → "./autolinux -c build" 선택
   ↓
2. axon.buildAutolinux 명령어 실행
   ↓
3. autolinux.config 읽기 (SDK, Machine 확인)
   ↓
4. SDK 템플릿에서 이미지 목록 로드
   ↓
5. QuickPick UI 표시
   ┌─────────────────────────────────────────────────┐
   │ Select an image to build                        │
   ├─────────────────────────────────────────────────┤
   │ > telechips-ivi-image-minimal     up-to-date    │
   │   telechips-ivi-image-multimedia  up-to-date    │
   │   telechips-ivi-image             up-to-date    │
   │   automotive-linux-platform-image up-to-date    │
   │   telechips-ces2024-demo-image    up-to-date    │
   └─────────────────────────────────────────────────┘
   ↓
6. 이미지 선택
   ↓
7. Terminal에서 빌드 실행
   ./autolinux -c build [selected-image]
```

#### 4. **주요 특징**

✅ **QuickPick UI**: VS Code 네이티브 UI로 빠른 선택
✅ **검색 기능**: 키보드로 이미지 이름 검색 가능
✅ **Main/Sub 자동 구분**: Machine에 따라 적절한 이미지 목록 표시
✅ **날짜 정보 표시**: 각 이미지의 유효 날짜 표시
✅ **Terminal 통합**: 빌드 진행 상황을 Terminal에서 실시간 확인
✅ **에러 처리**: autolinux.config 없음, 이미지 없음 등 명확한 에러 메시지

#### 5. **프로젝트 타입 처리**

**extension.ts - setProjectType 명령어**
```typescript
// yocto_autolinux를 yocto_project_autolinux로 정규화
let normalizedProjectType = projectType;
if (projectType === 'yocto_autolinux') {
    normalizedProjectType = 'yocto_project_autolinux';
}

// projectType을 직접 저장
await config.update('projectType', normalizedProjectType, vscode.ConfigurationTarget.Workspace);
```

**AxonSidebarProvider.ts - sendProjectType**
```typescript
// yocto_project_autolinux를 yocto_autolinux로 변환 (UI 호환성)
if (projectType === 'yocto_project_autolinux') {
    projectType = 'yocto_autolinux';
}
```

**저장 구조:**
- **Yocto Project**: `axon.projectType` = `yocto_project`
- **Yocto Project (autolinux)**: `axon.projectType` = `yocto_project_autolinux`
- **MCU Project**: `axon.projectType` = `mcu_project`

#### 6. **수정된 파일 (9개)**

1. **axon-sidebar.html**: Project Type 및 Build 메뉴 추가
2. **axon-sidebar.js**: UI 로직 및 이벤트 핸들러
3. **autolinux-creator.ts**: `loadImages()` 함수 추가
4. **autolinux-builder.ts**: 빌드 로직 구현 (신규)
5. **extension.ts**: `axon.buildAutolinux` 명령어 등록, `yocto_autolinux` 타입 처리
6. **AxonSidebarProvider.ts**: `sendProjectType()` 함수에서 autolinux 플래그 확인
7. **구현사항.md**: 빌드 기능 문서화

---

## 참고 사항

- **원격 환경 지원**: WSL/SSH 환경에서 동작하도록 `vscode.Uri` 사용
- **Python 3.6 호환성**: autolinux 스크립트는 Python 3.6+에서 실행
- **경로 처리**: vscode.workspace.fs API 사용으로 원격 경로 지원
- **메모리**: Python 3.6 호환을 위해 `subprocess.run()` 사용 시 주의
  - `capture_output` 대신 `stdout=subprocess.PIPE, stderr=subprocess.PIPE`
  - `text=True` 대신 `universal_newlines=True`
- **임시 파일 정리**: Python 실행 후 `.temp_*.py`, `.temp_*_output.json` 자동 삭제

