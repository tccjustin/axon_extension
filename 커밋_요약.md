# Git 커밋 요약

## 오늘 (2025-11-28) 커밋 요약

1. **Webview UI 개선 및 Yocto 빌더 리팩토링**: 프로젝트 타입 선택 기능 추가, Yocto 빌더/클린 공통 함수로 리팩토링, 성능 프로파일링 추가
2. **Yocto 빌드 작업 공통 함수 리팩토링**: buildAp/buildMcu/buildKernel 및 clean 함수들을 공통 함수 기반으로 통합, 코드 중복 대폭 감소
3. **MCU 빌드 기능 리팩토링**: extension.ts의 MCU 빌드 코드를 McuProjectBuilder 클래스로 이동하여 모듈별 책임 분리
4. **MCU Build 함수 리팩토링**: executeMcuTask 공통 함수 추가로 코드 중복 제거 (270줄 → 180줄, 33% 감소)
5. **Webview 상태 유지 및 성능 프로파일링**: 확장 전환 시 상태 유지 기능 추가, 성능 프로파일링 및 캐싱으로 최적화

---

## 어제 (2025-11-27) 커밋 요약

1. **사이드바 웹뷰 UI 기능 추가**: AxonSidebarProvider 클래스 및 웹뷰 UI 리소스 구현, Extension 초기화 시 웹뷰 등록
2. **inc 파일 업데이트 로직 개선**: 변수 값이 "${AUTOREV}"인 경우에만 업데이트 수행하도록 조건 추가
3. **Git 커밋 메시지 작성 규칙 파일 추가**: .cursorrules 파일 추가로 커밋 메시지 작성 규칙 및 TCS 티켓 번호 가이드 정의
4. **devtool modify MCU 지원 추가**: devtool modify 기능에서 MCU 레시피 지원 로직 추가, mcu-build.sh 스크립트 실행 연동
