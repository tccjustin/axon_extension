# Git Commit Message 템플릿

## 📝 커밋 메시지 작성 규칙

### 기본 형식
```
<type>: <subject>

<body>

<footer>
```

### Type 종류
- `feat`: 새로운 기능 추가
- `fix`: 버그 수정
- `docs`: 문서 수정
- `style`: 코드 포맷팅, 세미콜론 누락 등 (코드 변경 없음)
- `refactor`: 코드 리팩토링
- `test`: 테스트 코드 추가/수정
- `chore`: 빌드 업무, 패키지 매니저 설정 등
- `perf`: 성능 개선
- `ci`: CI/CD 관련 변경

### Subject (제목)
- 50자 이내
- 첫 글자는 대문자
- 마침표 없음
- 명령형으로 작성 ("Added" ❌, "Add" ✅)

### Body (본문)
- 72자마다 줄바꿈
- "무엇을", "왜" 변경했는지 설명
- "어떻게"는 코드에서 설명

### Footer (꼬리말)
- 이슈 트래커 ID 참조
- Breaking Changes 명시

---

## 📌 예시

### 기능 추가
```
feat: Add Yocto Project (autolinux) creation feature

- Add autolinux project creation dialog
- Implement autolinux configuration section
- Support SDK/Manifest/Machine selection
- Add automatic configure execution

Closes #123
```

### 버그 수정
```
fix: Fix SDK list not loading in autolinux dialog

Change vscode.Uri.file() to vscode.Uri.joinPath() 
to support remote environments (WSL/SSH)

The previous implementation only worked in local file system.

Fixes #456
```

### 리팩토링
```
refactor: Extract common shell execution logic

Move shell task execution to shell-utils.ts
for better code reusability
```

### 문서 작성
```
docs: Update implementation guide

- Add autolinux configuration implementation details
- Update .cursor_instructions.md with remote environment rules
- Create .cursorrules for automatic AI context
```

---

## ✅ 좋은 커밋 메시지

```
feat: Add user authentication

Implement JWT-based authentication system with:
- Login/logout endpoints
- Token refresh mechanism
- Role-based access control

Closes #789
```

## ❌ 나쁜 커밋 메시지

```
update

fixed bug

asdfasdf

WIP
```

---

## 💡 Tips

1. **커밋은 작게, 자주**: 하나의 논리적 변경만 포함
2. **의미 있는 제목**: 코드를 보지 않아도 이해 가능하게
3. **본문은 상세히**: 미래의 나를 위해
4. **이슈 연결**: 관련 이슈 번호 명시

---

## 🔧 Git Commit Template 설정 (선택사항)

```bash
# 이 파일을 git commit template으로 설정
git config --local commit.template .commit_template.md

# 커밋 시 자동으로 템플릿 표시
git commit
```

